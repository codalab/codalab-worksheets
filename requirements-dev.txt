# Required packages for contributing to CodaLab development.
# In particular, these packages are needed to run unit tests.
#
#   venv/bin/pip install -r ./requirements-dev.txt
#
-r requirements-server.txt
mock==1.3.0
nose==1.3.7
