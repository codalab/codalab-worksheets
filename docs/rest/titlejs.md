# `GET /titlejs/worksheet/<uuid:re:0x[0-9a-f]{32}>/`
    
Returns Javascript code that updates the worksheet title.

# `GET /titlejs/bundle/<uuid:re:0x[0-9a-f]{32}>/`
    
Returns Javascript code that updates the bundle title.

